<%= simple_form_for [@fortune, @comment] do |f| %>
  <%= f.input :body, :as => :text, :input_html => {:rows => 2, :cols => 72}, :label => '' %>
  <%= f.input :author, :input_html => {:size => 15}, :label => 'Author: '%>
  <%= f.button :submit %>
<% end %>